{
  "name": "agent",
  "version": "0.0.1",
  "description": "koa agent for Solid poll",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {}
}
