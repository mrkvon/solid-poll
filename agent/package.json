{
  "name": "agent",
  "version": "0.0.1",
  "description": "koa agent for Solid poll",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "koa": "^3.1.1"
  },
  "devDependencies": {
    "@types/koa": "^3.0.1",
    "@types/node": "^25.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
