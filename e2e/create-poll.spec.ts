import test from '@playwright/test'

test.describe('Create and manage poll', () => {
  test.fixme('select a new file on my pod to store the poll data', () => {})
  test.fixme('select a bot that will take care of the poll (or keep default bot)', () => {})
  test.fixme('write poll question', () => {})
  test.fixme('write detailed info or context or explanation about the question', () => {})
  test.fixme('save the poll to the file - data, inbox, access control allowing bot to write', () => {})
  test.fixme('update the poll', () => {})
  test.fixme('activate and desactivate the poll', () => {})
})
