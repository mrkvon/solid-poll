import test from '@playwright/test'

test.describe('View a poll', () => {
  test.fixme('see question, answers, and votes', () => {})
  test.fixme('sort answers by highest votes', () => {})
  test.fixme('sort answers by creation time (oldest or newest)', () => {})
})
